apiVersion: v1
kind: Service
metadata:
  name: receipt
  labels:
    app: receipt
spec:
  type: ClusterIP
  selector:
    app: receipt
  ports:
    - name: grpc
      port: 50053
      targetPort: 50053
