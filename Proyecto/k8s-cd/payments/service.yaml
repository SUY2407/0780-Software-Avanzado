apiVersion: v1
kind: Service
metadata:
  name: payment-service
spec:
  type: ClusterIP
  selector:
    app: payment-service
  ports:
    - name: rest
      port: 8000
      targetPort: rest
    - name: grpc
      port: 8082
      targetPort: grpc
